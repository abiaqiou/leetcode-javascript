const mergeTwoLists = require('../../problems/linked_list/0021_merge_two_sorted_lists')

function ListNode(val, next) {
  this.val = val === undefined ? 0 : val
  this.next = next === undefined ? null : next
}

test('0021 mergeTwoLists test 01', () => {
  // todo
})

test('0021 mergeTwoLists test 02', () => {
  // todo
})

test('0021 mergeTwoLists test 03', () => {
  // todo
})
